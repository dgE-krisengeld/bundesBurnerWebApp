import React from 'react';

export const Icon = props => {
  const {type = 'solid', name, ...rest} = props;

  if (type === 'solid') {
    return <div>make it icon</div>;
  }

  return <div>make it icon</div>;
};
